import { BasketProductLinePricePipe } from './basket-product-line-price.pipe';

describe('BasketProductLinePricePipe', () => {
  it('create an instance', () => {
    const pipe = new BasketProductLinePricePipe();
    expect(pipe).toBeTruthy();
  });
});
