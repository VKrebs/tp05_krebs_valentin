import { BasketTotalPricePipe } from './basket-total-price.pipe';

describe('BasketTotalPricePipe', () => {
  it('create an instance', () => {
    const pipe = new BasketTotalPricePipe();
    expect(pipe).toBeTruthy();
  });
});
